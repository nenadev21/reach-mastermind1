import React, { useState } from 'react';
import { Button, Header, Icon } from 'semantic-ui-react';

const UserGuesses = ({ random }) => {
  return <div className='user-guesses-container'></div>;
};

export default UserGuesses;
